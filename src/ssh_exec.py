"""Removed SSH functionality.
This stub is left only to avoid import errors if something still references ssh_exec.
All SSH features were eliminated. Use main.py (WoL only).
"""

raise SystemExit("ssh_exec.py удалён: функционал SSH исключён.")
